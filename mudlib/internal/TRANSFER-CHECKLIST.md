# AetherMUD WSL → Fedora Workstation Transfer Checklist

Generated 2026-07-11 from a full audit of the production package.
Canonical archive: `aethermud-full-20260711.tar.gz` (SHA256 verified OK on WSL).

## 1. Production archive

- [ ] Copy `production-aethermud/` folder (whole folder, not just the tarball) to HDD
- [ ] `aethermud-full-20260711.tar.gz` present on HDD
- [ ] `aethermud-full-20260711.tar.gz.sha256` present on HDD
- [ ] After copy to new machine: `sha256sum -c aethermud-full-20260711.tar.gz.sha256` prints OK

## 2. Docs bundle (already inside production-aethermud/, verified identical to project-root copies)

- [ ] `install-aethermud.md` (the Fedora-specific guide - follow this one)
- [ ] `INSTALL.md`
- [ ] `RUNNING.md`
- [ ] `README.md`
- [ ] `SHARING.md`
- [ ] `TRANSFER-CHECKLIST.md` (this file)

## 3. lpc-archive/ - NOT in the production archive, separate copy required

- [ ] Copy `/home/thurtea/lpc-archive/` to HDD (7 Rifts_LPC_* snapshot dirs)
- [ ] Copy to `~/lpc-archive/` on the new machine

## 4. Dotfiles - NOT in the production archive, manual copy + careful merge

Do NOT blindly overwrite; Fedora ships its own defaults and .claude.json
holds machine-specific state. Copy to HDD, then merge by hand:

- [ ] `~/.bashrc` (merge custom aliases/exports into Fedora's stock .bashrc)
- [ ] `~/.bash_profile` (same - merge, don't replace)
- [ ] `~/.gitconfig` (usually safe to copy whole; check paths in any includeIf)
- [ ] `~/.claude.json` (review before copying; contains per-machine session state)

## 5. Website (www/) - IN the archive, but that does not deploy it

`aethermud/www/` (9 files: index, admin, apprentice, chargen, coding,
domain, qcs, roadmap, roleplay) is inside the tarball, so it lands on the
new machine automatically. NOTE: `apprentice.html` and the updated
`index.html` were added AFTER the 20260711 tarball was built - rebuild the
archive (recipe in install-aethermud.md) before the HDD copy, or copy
`www/` separately. The live site at https://aethermud.com/ is a whole
generation behind the local copy and must be redeployed:

- [x] Rebuild `aethermud-full-YYYYMMDD.tar.gz` + .sha256 so it includes
      `www/apprentice.html` and the updated `www/index.html`
      (done 2026-07-11 13:35; new SHA256 starts 0932a0a8, verified OK)
- [x] All 9 pages verified LIVE 2026-07-11 (deployed 16:30 UTC by an
      as-yet-unidentified method - no deploy script/credentials exist in
      this WSL environment). Content matches local exactly EXCEPT the
      deployed copies link a new shared `css/site.css` (dark staff-portal
      theme, includes webclient styles) instead of the local inline CSS.
- [x] `roadmap.html` and `apprentice.html` - confirmed live, HTTP 200
- [x] Styling decision RESOLVED 2026-07-11: live css/site.css is canonical.
      Live www synced back into `~/aethermud/www/` (9 pages byte-identical
      to live, plus `css/site.css`, SHA256 ebc84692...). www/ is now 9 html
      + css/site.css + .htaccess.
- [ ] Upload `www/.htaccess` (created locally 2026-07-11) to the docroot:
      301-redirects info.html -> index.html, staff.html -> admin.html,
      server.html -> index.html. NOT yet on the live host - no deploy
      credentials exist in this WSL environment.
- [ ] After upload, verify: `curl -sI https://aethermud.com/info.html`
      returns 301 with Location: /index.html (same for staff/server)
- [ ] Optionally delete the three legacy html files from the docroot once
      redirects are confirmed (redirect rules win either way). Do NOT
      delete `css/site.css` - the whole site depends on it.
- [ ] Tarball is STALE AGAIN: `aethermud-full-20260711.tar.gz` (built
      13:35) predates the www sync-back (15:04). Rebuild before the HDD
      copy so the archive carries the final website.
- [ ] Document the actual deploy method/credentials in PUBLISHING.md
      (nothing in the repo says how the site is published)

## 6. Files excluded from the archive (intentional - runtime state only)

Nothing unexpected is missing. The only aethermud/ paths absent from the
tarball are the documented runtime exclusions; they regenerate on first start:

- [x] `nightmare3_fluffos_v2/bin/mud.pid` (regenerated by mud.sh)
- [x] `nightmare3_fluffos_v2/bin/addr.pid` (regenerated by mud.sh)
- [x] `nightmare3_fluffos_v2/lib/log/runtime` (regenerated at boot)

Older archives `~/aethermud-20260708.tar.gz` and
`~/aethermud-full-20260710.tar.gz` are superseded - do not transfer them
(or keep one on the HDD as a fallback, clearly labeled old).

## 7. FluffOS driver rebuild on the new Fedora host

- [ ] `sudo dnf groupinstall "Development Tools" -y`
- [ ] `sudo dnf install gcc gcc-c++ make bison flex openssl-devel pcre-devel libevent-devel jemalloc-devel sqlite-devel libpq-devel mariadb-connector-c-devel telnet -y`
- [ ] `cd ~/aethermud/nightmare3_fluffos_v2/fluffos-2.9-ds2.08 && ./configure nm3 && make && make install`
- [ ] Confirm `driver`, `addr_server`, `portbind` landed in `nightmare3_fluffos_v2/bin/`

## 8. mud.sh and paths

- [ ] `chmod +x ~/aethermud/mud.sh ~/aethermud/scripts/*.sh` after extract
- [ ] mud.sh itself is path-portable (derives everything from its own location)
- [ ] BUT `nightmare3_fluffos_v2/bin/mudos.cfg` hardcodes
      `/home/thurtea/aethermud/...` - correct as-is if the new username is
      `thurtea` and you extract into `~`; otherwise edit the two path lines
      or run `./mud.sh init-config`
- [ ] `./mud.sh doctor` reports no [FAIL] lines

## 9. Firewall (LAN access)

- [ ] `sudo firewall-cmd --permanent --add-port=1122/tcp` (player telnet)
- [ ] `sudo firewall-cmd --permanent --add-port=1129/tcp` (websocket)
- [ ] `sudo firewall-cmd --reload`
- [ ] Do NOT open 1199 (addr_server is driver-internal)

## 10. First-run validation

- [ ] `cd ~/production-aethermud && sha256sum -c aethermud-full-20260711.tar.gz.sha256`
- [ ] `cd ~ && tar xzf ~/production-aethermud/aethermud-full-20260711.tar.gz`
- [ ] Driver rebuilt (section 7)
- [ ] `cd ~/aethermud && ./mud.sh start` (preload takes 1-3 min)
- [ ] `ss -tlnp | grep 1122` shows the driver listening
- [ ] `telnet localhost 1122` and log in with an existing character
      (player saves are in the tarball at `lib/secure/save/users/`)
