Read /CLAUDE.md first, then this directory's README.md, then the parent
www/ LLM_BREADCRUMB.md. Gotchas: AetherMUD.jpeg is the landing hero
image referenced by www/index.html; favicons are referenced by every
page. Do not rename files here without updating the referencing HTML.
Deploys are a manual copy to public_html on the VPS (see CLAUDE.md).
