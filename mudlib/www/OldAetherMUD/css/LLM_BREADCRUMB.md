# LLM breadcrumb - www/css/

1. Read `/CLAUDE.md` at the repo root first.
2. Read `README.md` in this directory (above).
3. Check the parent `www/LLM_BREADCRUMB.md` (and the repo root's) before
   assuming this directory's gotchas are self-contained - this directory
   has none beyond what the parent already covers.
4. Related: `www/` for the HTML pages this stylesheet serves.

## Gotchas specific to this directory

- Single shared stylesheet - changes here affect every page under
  `www/`, not just one. Check multiple pages after editing `site.css`.
