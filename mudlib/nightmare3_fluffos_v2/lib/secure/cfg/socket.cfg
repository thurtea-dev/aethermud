# /adm/db/preload_socket.db
# from Nightmare IV
# These are the network related preload files
# They must preload later than other objects because the driver
# is not ready to handle sockets at the time of preload.
#
# network and http bind extra local ports derived from the telnet port
# (see secure/include/network.h). Only addr_server (1199) and telnet
# (1122) are used on this MUD.

# /daemon/network
/daemon/services
# /daemon/http
