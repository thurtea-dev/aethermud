# win32/

Prebuilt Windows driver binary and launcher, for players/developers
running AetherMUD on Windows instead of Linux.

- `driver.exe` - prebuilt Windows driver binary.
- `mud.bat` - Windows launcher batch script (the Windows equivalent of
  the repo-root `mud.sh`).
